# Todo Fixture

A todo-list service. Users create todos, organize them into subtasks, and move them
through a lifecycle. The service enforces a handful of business rules around completion,
overdue handling, priority, per-user limits, and deletion.

## Running

```bash
npm install
npm run build
npm start
```

The HTTP server listens on port 3000.
